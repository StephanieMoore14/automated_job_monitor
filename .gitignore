# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
env/

# State file — generated on every run, should not be committed
whoop_jobs_data.json

# Local / secrets (if you ever move credentials to a file)
.env
.env.local
*.local
